const sendOTP = async(({email,subject,message,duration=1})=>{
    try{
        if(!(email && subject && message)){
            throw Error("Provide values for subject message and email");
        }
        await 
    }
    catch(err)
    {
        
    }
})